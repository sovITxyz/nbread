### P1 — Nostr core (M)
**Depends**: P0. **Touches only** `src/nostr/`, tests.
**Deliverables**: `event.ts` (canonical serialization → sha256 id, schnorr verify), `nip19.ts` (npub/naddr), `relay.ts` (WS pool: connect, REQ with filter, collect until EOSE or timeout, dedupe by id, close; per-relay failure isolation), replaceable-resolution helper (newest per pubkey/kind/d_tag).
**Tests**: fixture suite — valid pass, tampered fail (each tamper class); nip19 round-trips + invalid bech32 rejected; relay client vs in-test mock WS server (miniflare supports WS): dedupe, timeout, one-dead-relay tolerance; replaceable resolution incl. tie-break.
**Accept**: 100% of fixture cases correct; no network calls in tests.

