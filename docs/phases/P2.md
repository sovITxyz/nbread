### P2 — Rendering (M)
**Depends**: P1 (types only). **Touches only** `src/markdown/`, `src/views/`, `src/routes/tenant.ts` (render-only, data injected), tests.
**Deliverables**: markdown pipeline (markdown-it + footnote/task-lists/mark/sup/sub plugins, highlight.js via highlight callback, heading slug ids; `html:false` — raw HTML escaped) + `sanitize.ts` output allowlist pass; `css-sanitize.ts` (strip `@import`, `url(`, `expression`, `</style>` breakout) for per-blog theme CSS; views: base layout, blog home (post list), post page, RSS/Atom, sitemap.xml, robots.txt, 404; tenant routes rendering from injected fixture events.
**Tests**: snapshot corpus — every markdown feature + every XSS payload fixture neutralized (assert no `<script`, no `on*=` attrs, no `javascript:` hrefs in output); CSS sanitizer cases; RSS/sitemap well-formed XML; render CPU sanity (<5ms on torture post).
**Accept**: all snapshots + XSS assertions green; reviewer B signs off on sanitizer.

