### P4 — Auth + handle claim + NIP-05 (M)
**Depends**: P0 (sessions contract), P1 (verify).
**Deliverables**: `routes/auth.ts` — GET login page (+ `public/login.js` NIP-07 glue), nonce in KV (5min, single-use), POST verifies kind 22242 event (sig, nonce tag, created_at ±10min, pubkey) → session; logout; `routes/dashboard.ts` claim flow (Turnstile siteverify, reserved list, UNIQUE race-safe, one handle per pubkey, handle regex `^[a-z0-9][a-z0-9-]{1,30}$`); `routes/wellknown.ts` `/.well-known/nostr.json?name=` (CORS `*`); rate limits (login 10/15min/IP, claim 3/h/IP) via the D1 `rate_limits` table (NOT KV — free-tier KV write budget).
**Tests**: forged sig / expired / replayed nonce / wrong-pubkey all rejected; session cookie flags (HttpOnly, Secure, SameSite=Lax, host-only); concurrent duplicate claim → exactly one wins; reserved + invalid handles rejected; NIP-05 JSON shape + CORS header; rate limit trips.
**Accept**: reviewer B signs off on auth (fixation, replay, enumeration).

