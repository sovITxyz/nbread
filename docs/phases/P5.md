### P5 — Editor + dashboard (L)
**Depends**: P3, P4.
**Deliverables**: dashboard (post list from own mirrored events, settings form: theme CSS via css-sanitize, about, relay list into `users.settings`); editor page + `public/editor.js` (build kind 30023 with d-tag slug/title/summary/published_at tags, `window.nostr.signEvent`, broadcast to relays client-side, POST signed event to `/api/mirror`); `routes/api.ts` mirror endpoint (session required, `ev.pubkey === session.pubkey`, then `mirrorEvent`); server-rendered preview endpoint using `renderPost`; delete button → signed kind 5 via same path.
**Tests**: mirror API rejects: no session, pubkey mismatch (mallory event on alice session), invalid sig; edit produces replaceable update visible on blog; preview HTML === published HTML for same content; settings CSS sanitized; CSRF (Origin check) on all dashboard POSTs.
**Accept**: tenant isolation proven (A cannot publish/delete as B); full write→render loop works in `wrangler dev` with a real NIP-07 extension (manual check noted in smoke doc).

