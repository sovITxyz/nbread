/**
 * Sanitizer for per-blog theme CSS: strips `@import`, `url(`, `expression`,
 * and `</style>` breakout attempts. Implemented in P2 (rendering).
 */
export function sanitizeCss(_css: string): string {
  throw new Error("Not implemented until P2 (rendering)");
}
